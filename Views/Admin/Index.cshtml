@model List<BACommerce.Models.Admin>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<html>

<body>
    <div class="container">
        <div class="row">
            <div class="panel panel-default">
                <div class="panel-body">
                    <div class="col-lg-12">

                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Adı</th>
                                    <th>Şifre</th>
                                    <th>Yetki</th>
                                    <th>İşlemler</th>
                                </tr>
                            </thead>

                            <tbody>
                                @foreach (BACommerce.Models.Admin admin in Model)
                                {
                                    <tr>
                                        <td>@admin.AdminID</td>
                                        <td>@admin.AdminName</td>
                                        <td>@admin.Password</td>
                                        <td>@admin.Rights</td>
                                        <td>
                                           
                                            <a href='~/Admin/Guncelle/@admin.AdminID' class="btn btn-warning">Güncelle</a>
                                            <a href='~/Admin/Sil/@admin.AdminID' class="btn btn-danger">Sil</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>

                        </table>

                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>

